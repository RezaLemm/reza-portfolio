"use client"

import Link from "next/link"
import {motion, useMotionValue, useReducedMotion, useSpring, useTransform} from "framer-motion"
import {
  ArrowUpRight,
  BadgeCheck,
  Crown,
  Diamond,
  Eye,
  Gem,
  Image as ImageIcon,
  Layers3,
  MousePointer2,
  Palette,
  Sparkles,
  Wand2,
  Zap,
} from "lucide-react"
import {useEffect, useMemo, useState} from "react"

type Lang = "fa" | "en"

const easePremium = [0.16, 1, 0.3, 1] as const

const copy = {
  fa: {
    eyebrow: "پورتفولیوی طراحی گرافیک و آثار بصری",
    titleA: "LEMM Studio",
    titleB: "استودیوی طراحی",
    titleC: "بصری",
    description:
      "یک تجربه بصری لوکس برای نمایش هویت برند، بنر، تامبنیل، آرت‌ورک‌های AI، طراحی تبلیغاتی و ارائه‌های حرفه‌ای.",
    primary: "مشاهده کانواس",
    secondary: "شروع همکاری",
    mini: "طراحی‌هایی با حس سینمایی، جزئیات تمیز، تایپوگرافی قوی و خروجی آماده ارائه.",
    ribbon: ["Brand Identity", "Visual Design", "AI Artwork", "Campaign Visuals", "Premium Presentation"],
    stats: [
      ["+30", "پروژه منتخب"],
      ["4", "حوزه تخصصی"],
      ["AI", "فرآیند خلاق"],
    ],
    orbit: ["Branding", "Visuals", "AI", "Canvas"],
    panelLabel: "LEMM Studio",
    panelTitle: "Selected Visual Works",
    panelDesc: "هویت بصری، تامبنیل، بنر، آرت‌ورک و تجربه‌های خلاقانه.",
    cards: [
      {
        title: "هویت بصری",
        text: "طراحی زبان تصویری منسجم برای برندهای شخصی، تجاری و دیجیتال.",
      },
      {
        title: "تبلیغات و بنر",
        text: "ساخت تصویرهای تبلیغاتی با تمرکز روی جذب نگاه، وضوح پیام و حس لوکس.",
      },
      {
        title: "AI Artwork",
        text: "ترکیب ایده‌پردازی، طراحی و هوش مصنوعی برای خروجی‌های متفاوت.",
      },
    ],
    process: [
      ["01", "Visual Direction", "مسیر بصری و mood اصلی پروژه مشخص می‌شود."],
      ["02", "Design System", "رنگ، تایپوگرافی، ترکیب‌بندی و قواعد تصویری ساخته می‌شود."],
      ["03", "Premium Output", "خروجی نهایی با جزئیات تمیز و آماده استفاده تحویل می‌شود."],
    ],
    ctaTitle: "یک پروژه بصری خاص داری؟",
    ctaText: "برای طراحی برند، بنر، تامبنیل، آرت‌ورک یا پرزنتیشن حرفه‌ای آماده‌ام.",
    ctaButton: "تماس",
  },
  en: {
    eyebrow: "Graphic Design & Visual Portfolio",
    titleA: "LEMM Studio",
    titleB: "Visual",
    titleC: "Portfolio",
    description:
      "A luxury visual experience for brand identity, banners, thumbnails, AI artworks, campaign design, and premium presentation systems.",
    primary: "View Canvas",
    secondary: "Start Project",
    mini: "Cinematic visuals, refined details, strong typography, and presentation-ready design outputs.",
    ribbon: ["Brand Identity", "Visual Design", "AI Artwork", "Campaign Visuals", "Premium Presentation"],
    stats: [
      ["+30", "Selected works"],
      ["4", "Design areas"],
      ["AI", "Creative workflow"],
    ],
    orbit: ["Branding", "Visuals", "AI", "Canvas"],
    panelLabel: "LEMM Studio",
    panelTitle: "Selected Visual Works",
    panelDesc: "Brand identity, thumbnails, banners, AI visuals, and creative design studies.",
    cards: [
      {
        title: "Brand Identity",
        text: "Building a refined visual language for personal, commercial, and digital brands.",
      },
      {
        title: "Campaign Visuals",
        text: "Advertising visuals focused on attention, clarity, and a premium visual tone.",
      },
      {
        title: "AI Artwork",
        text: "Creative direction, design taste, and AI-assisted production for distinctive outputs.",
      },
    ],
    process: [
      ["01", "Visual Direction", "The core mood, visual style, and direction are defined."],
      ["02", "Design System", "Color, typography, composition, and image rules are shaped."],
      ["03", "Premium Output", "The final work is polished, clean, and ready for use."],
    ],
    ctaTitle: "Have a visual project in mind?",
    ctaText: "Branding, banners, thumbnails, AI artwork, and premium presentation design.",
    ctaButton: "Contact",
  },
} satisfies Record<Lang, any>

function getLang(): Lang {
  if (typeof document === "undefined") return "en"

  const htmlLang = document.documentElement.getAttribute("lang")?.toLowerCase() || ""
  const htmlDir = document.documentElement.getAttribute("dir")?.toLowerCase() || ""
  const bodyDir = document.body?.getAttribute("dir")?.toLowerCase() || ""

  if (htmlLang.startsWith("fa") || htmlDir === "rtl" || bodyDir === "rtl") return "fa"
  return "en"
}

export default function PremiumHomePage() {
  const shouldReduceMotion = useReducedMotion()
  const [lang, setLang] = useState<Lang>("en")

  const mouseX = useMotionValue(0)
  const mouseY = useMotionValue(0)

  const smoothX = useSpring(mouseX, {stiffness: 90, damping: 28, mass: 0.6})
  const smoothY = useSpring(mouseY, {stiffness: 90, damping: 28, mass: 0.6})

  const cardRotateY = useTransform(smoothX, [0, 1], [8, -8])
  const cardRotateX = useTransform(smoothY, [0, 1], [-7, 7])

  useEffect(() => {
    const updateLang = () => setLang(getLang())
    updateLang()

    const observer = new MutationObserver(updateLang)

    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["lang", "dir"],
    })

    if (document.body) {
      observer.observe(document.body, {
        attributes: true,
        attributeFilter: ["dir", "lang"],
      })
    }

    return () => observer.disconnect()
  }, [])

  const isFa = lang === "fa"
  const t = copy[lang]

  const particles = useMemo(
    () =>
      Array.from({length: 58}, (_, index) => ({
        id: index,
        left: `${2 + ((index * 37) % 96)}%`,
        top: `${5 + ((index * 53) % 86)}%`,
        size: index % 8 === 0 ? 2.3 : index % 3 === 0 ? 1.55 : 1.05,
        delay: (index % 13) * 0.08,
        opacity: index % 6 === 0 ? 0.52 : 0.22,
      })),
    []
  )

  const handlePointerMove = (event: React.PointerEvent<HTMLElement>) => {
    const rect = event.currentTarget.getBoundingClientRect()
    mouseX.set((event.clientX - rect.left) / rect.width)
    mouseY.set((event.clientY - rect.top) / rect.height)
  }

  return (
    <main
      dir={isFa ? "rtl" : "ltr"}
      onPointerMove={handlePointerMove}
      className="relative isolate min-h-screen overflow-hidden bg-[#030303] text-white"
    >
      <div className="pointer-events-none absolute inset-0 -z-30 bg-[radial-gradient(circle_at_50%_18%,rgba(255,234,202,0.14),transparent_28%),radial-gradient(circle_at_15%_20%,rgba(211,151,72,0.18),transparent_35%),radial-gradient(circle_at_85%_74%,rgba(128,72,29,0.16),transparent_36%),linear-gradient(180deg,#050505_0%,#080706_46%,#020202_100%)]" />

      <motion.div
        className="pointer-events-none fixed inset-0 -z-20 opacity-70"
        style={{
          background: useTransform(
            [smoothX, smoothY],
            ([x, y]) =>
              `radial-gradient(circle at ${Number(x) * 100}% ${Number(y) * 100}%, rgba(215,176,106,0.16), transparent 32%)`
          ),
        }}
      />

      <div className="pointer-events-none absolute inset-0 -z-20 opacity-[0.055] [background-image:linear-gradient(rgba(255,255,255,0.7)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.7)_1px,transparent_1px)] [background-size:118px_118px]" />

      <div className="pointer-events-none absolute left-1/2 top-[40%] -z-10 h-[92vmin] w-[92vmin] -translate-x-1/2 -translate-y-1/2 rounded-full border border-[#d7b06a]/10" />
      <div className="pointer-events-none absolute left-1/2 top-[40%] -z-10 h-[68vmin] w-[68vmin] -translate-x-1/2 -translate-y-1/2 rounded-full border border-[#d7b06a]/10" />
      <div className="pointer-events-none absolute left-1/2 top-[40%] -z-10 h-[44vmin] w-[44vmin] -translate-x-1/2 -translate-y-1/2 rounded-full border border-[#d7b06a]/10" />

      {!shouldReduceMotion &&
        particles.map((item) => (
          <motion.span
            key={item.id}
            className="pointer-events-none absolute z-0 rounded-full bg-[#f5d99c]"
            style={{
              left: item.left,
              top: item.top,
              width: item.size,
              height: item.size,
              boxShadow: "0 0 20px rgba(215,176,106,0.42)",
            }}
            initial={{opacity: 0, scale: 0.7}}
            animate={{
              opacity: [0, item.opacity, 0],
              scale: [0.7, 1.15, 0.7],
              y: [-4, 8, -4],
            }}
            transition={{
              duration: 4.6,
              delay: item.delay,
              repeat: Infinity,
              ease: "easeInOut",
            }}
          />
        ))}

      <section className="relative mx-auto grid min-h-screen w-full max-w-[1220px] grid-cols-1 items-center gap-16 px-6 pb-24 pt-28 md:grid-cols-[1fr_0.98fr] md:px-8 lg:gap-24">
        <motion.div
          className={isFa ? "text-center md:text-right" : "text-center md:text-left"}
          initial={{opacity: 0, y: 26}}
          animate={{opacity: 1, y: 0}}
          transition={{duration: 0.8, ease: easePremium}}
        >
          <motion.div
            className="mb-7 inline-flex items-center gap-3 rounded-full border border-[#d7b06a]/25 bg-white/[0.04] px-4 py-2 text-[11px] font-black uppercase tracking-[0.3em] text-[#d7b06a] shadow-[0_22px_90px_rgba(0,0,0,0.46)] backdrop-blur-xl"
            initial={{opacity: 0, y: 12}}
            animate={{opacity: 1, y: 0}}
            transition={{duration: 0.55, delay: 0.12, ease: easePremium}}
          >
            <Sparkles size={14} />
            <span>{t.eyebrow}</span>
          </motion.div>

          <div className="relative">
            <motion.div
              className={isFa ? "absolute -right-5 top-1 hidden h-full w-px bg-gradient-to-b from-transparent via-[#d7b06a] to-transparent md:block" : "absolute -left-5 top-1 hidden h-full w-px bg-gradient-to-b from-transparent via-[#d7b06a] to-transparent md:block"}
              initial={{scaleY: 0, opacity: 0}}
              animate={{scaleY: 1, opacity: 1}}
              transition={{duration: 0.75, delay: 0.35, ease: easePremium}}
            />

            <h1 className="relative max-w-[780px] text-[clamp(3.75rem,8.35vw,8.3rem)] font-semibold leading-[0.86] tracking-[-0.083em] text-white md:max-w-none">
              <span className="block">{t.titleA}</span>
              <span className="mt-4 block text-white/95">{t.titleB}</span>
              <span className="block bg-gradient-to-b from-white via-white to-white/58 bg-clip-text text-transparent">
                {t.titleC}
              </span>
            </h1>

            <motion.div
              className="mt-7 h-px w-full max-w-[620px] bg-gradient-to-r from-transparent via-[#d7b06a]/70 to-transparent"
              initial={{scaleX: 0, opacity: 0}}
              animate={{scaleX: 1, opacity: 1}}
              transition={{duration: 0.75, delay: 0.45, ease: easePremium}}
            />
          </div>

          <p className="mt-8 max-w-[630px] text-base leading-8 text-white/58 md:text-lg">
            {t.description}
          </p>

          <div className="mt-5 flex max-w-[650px] items-start gap-3 rounded-3xl border border-white/10 bg-white/[0.035] p-4 text-sm leading-7 text-white/56 backdrop-blur-xl">
            <BadgeCheck className="mt-1 shrink-0 text-[#d7b06a]" size={18} />
            <span>{t.mini}</span>
          </div>

          <div className="mt-9 flex flex-wrap items-center justify-center gap-4 md:justify-start">
            <Link
              href="/work"
              className="group relative inline-flex h-14 items-center gap-3 overflow-hidden border border-[#d7b06a] bg-[#d7b06a] px-7 text-sm font-black text-black transition duration-300 hover:bg-[#f1cf8b]"
            >
              <span className="absolute inset-0 -translate-x-full bg-gradient-to-r from-transparent via-white/40 to-transparent transition duration-700 group-hover:translate-x-full" />
              <span className="relative">{t.primary}</span>
              <ArrowUpRight size={17} className="relative transition duration-300 group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />
            </Link>

            <Link
              href="/contact"
              className="group inline-flex h-14 items-center gap-3 border border-white/12 bg-white/[0.02] px-7 text-sm font-black text-white transition duration-300 hover:border-[#d7b06a]/50 hover:bg-white/[0.06]"
            >
              <span>{t.secondary}</span>
              <ArrowUpRight size={17} className="transition duration-300 group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />
            </Link>
          </div>

          <div className="mt-10 grid max-w-[640px] grid-cols-3 gap-3">
            {t.stats.map(([value, label]: [string, string], index: number) => (
              <motion.div
                key={label}
                className="rounded-3xl border border-white/10 bg-white/[0.035] px-4 py-5 text-center shadow-[0_24px_80px_rgba(0,0,0,0.36)] backdrop-blur-xl"
                initial={{opacity: 0, y: 12}}
                animate={{opacity: 1, y: 0}}
                transition={{duration: 0.5, delay: 0.46 + index * 0.07, ease: easePremium}}
              >
                <div className="text-2xl font-black text-[#d7b06a]">{value}</div>
                <div className="mt-1 text-[11px] font-semibold text-white/48">{label}</div>
              </motion.div>
            ))}
          </div>
        </motion.div>

        <motion.div
          className="relative mx-auto w-full max-w-[610px] [perspective:1200px]"
          initial={{opacity: 0, y: 34, scale: 0.985}}
          animate={{opacity: 1, y: 0, scale: 1}}
          transition={{duration: 0.85, delay: 0.18, ease: easePremium}}
        >
          <div className="absolute -inset-12 rounded-[58px] bg-[radial-gradient(circle_at_50%_45%,rgba(255,235,200,0.16),transparent_58%)] blur-2xl" />

          {!shouldReduceMotion && (
            <motion.div
              className="absolute left-1/2 top-1/2 h-[118%] w-[118%] -translate-x-1/2 -translate-y-1/2 rounded-full border border-[#d7b06a]/12"
              animate={{rotate: 360}}
              transition={{duration: 30, repeat: Infinity, ease: "linear"}}
            >
              {t.orbit.map((item: string, index: number) => (
                <div
                  key={item}
                  className="absolute rounded-full border border-white/10 bg-black/55 px-3 py-1 text-[10px] font-bold uppercase tracking-[0.18em] text-white/58 backdrop-blur-xl"
                  style={{
                    left: index === 0 ? "50%" : index === 1 ? "88%" : index === 2 ? "12%" : "50%",
                    top: index === 0 ? "-14px" : index === 1 ? "50%" : index === 2 ? "50%" : "calc(100% - 6px)",
                    transform: "translate(-50%, -50%)",
                  }}
                >
                  {item}
                </div>
              ))}
            </motion.div>
          )}

          <motion.div
            className="relative overflow-hidden rounded-[38px] border border-white/10 bg-white/[0.06] p-4 shadow-[0_48px_160px_rgba(0,0,0,0.72)] backdrop-blur-2xl"
            style={{
              rotateX: shouldReduceMotion ? 0 : cardRotateX,
              rotateY: shouldReduceMotion ? 0 : cardRotateY,
              transformStyle: "preserve-3d",
            }}
          >
            <div className="relative overflow-hidden rounded-[28px] border border-white/10 bg-[#10100f] px-7 py-24">
              <div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_34%,rgba(215,176,106,0.22),transparent_34%),linear-gradient(135deg,rgba(255,255,255,0.065),transparent_34%,rgba(255,255,255,0.04))]" />
              <div className="absolute left-1/2 top-1/2 h-72 w-72 -translate-x-1/2 -translate-y-1/2 rounded-full border border-[#d7b06a]/10" />
              <div className="absolute left-1/2 top-1/2 h-44 w-44 -translate-x-1/2 -translate-y-1/2 rounded-full border border-[#d7b06a]/10" />
              <div className="absolute left-6 bottom-6 h-16 w-16 rounded-full border border-[#d7b06a]/25" />
              <div className="absolute right-8 top-8 h-3 w-3 rounded-full bg-[#d7b06a] shadow-[0_0_30px_rgba(215,176,106,0.9)]" />

              <div className="relative z-10 mx-auto max-w-[450px] text-center">
                <div className="mb-6 flex items-center justify-center gap-3 text-[11px] font-black uppercase tracking-[0.55em] text-[#d7b06a]">
                  <Gem size={15} />
                  <span>{t.panelLabel}</span>
                </div>
                <h2 className="text-[clamp(2.25rem,3.8vw,3.7rem)] font-semibold leading-[1.02] tracking-[-0.055em]">
                  {t.panelTitle}
                </h2>
                <p className="mx-auto mt-6 max-w-[370px] text-sm leading-7 text-white/54">
                  {t.panelDesc}
                </p>
              </div>
            </div>
          </motion.div>

          <motion.div
            className="absolute -bottom-8 left-1/2 grid w-[90%] -translate-x-1/2 grid-cols-3 gap-3"
            initial={{opacity: 0, y: 18}}
            animate={{opacity: 1, y: 0}}
            transition={{duration: 0.6, delay: 0.58, ease: easePremium}}
          >
            {[Eye, Diamond, Zap].map((Icon, index) => (
              <div
                key={index}
                className="flex h-20 items-center justify-center rounded-3xl border border-white/10 bg-[#090908]/82 text-[#d7b06a] shadow-[0_24px_70px_rgba(0,0,0,0.5)] backdrop-blur-xl"
              >
                <Icon size={23} />
              </div>
            ))}
          </motion.div>
        </motion.div>
      </section>

      <div className="relative mx-auto mb-16 flex w-full max-w-[1180px] overflow-hidden border-y border-white/10 py-4">
        <motion.div
          className="flex min-w-max items-center gap-10 text-[12px] font-black uppercase tracking-[0.35em] text-[#d7b06a]/80"
          animate={shouldReduceMotion ? undefined : {x: isFa ? ["0%", "50%"] : ["0%", "-50%"]}}
          transition={{duration: 22, repeat: Infinity, ease: "linear"}}
        >
          {[...t.ribbon, ...t.ribbon, ...t.ribbon].map((item: string, index: number) => (
            <span key={`${item}-${index}`} className="flex items-center gap-10">
              {item}
              <Sparkles size={13} />
            </span>
          ))}
        </motion.div>
      </div>

      <section className="relative mx-auto grid w-full max-w-[1180px] grid-cols-1 gap-4 px-6 pb-16 md:grid-cols-3 md:px-8">
        {t.cards.map((item: {title: string; text: string}, index: number) => {
          const icons = [Palette, ImageIcon, Wand2]
          const Icon = icons[index] || Layers3

          return (
            <motion.div
              key={item.title}
              className="group relative min-h-[250px] overflow-hidden rounded-[32px] border border-white/10 bg-white/[0.035] p-7 backdrop-blur-xl transition duration-500 hover:-translate-y-1 hover:border-[#d7b06a]/35 hover:bg-white/[0.06]"
              initial={{opacity: 0, y: 24}}
              whileInView={{opacity: 1, y: 0}}
              viewport={{once: true, margin: "-120px"}}
              transition={{duration: 0.58, delay: index * 0.08, ease: easePremium}}
            >
              <div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_0%,rgba(215,176,106,0.16),transparent_38%)] opacity-0 transition duration-500 group-hover:opacity-100" />
              <div className="absolute bottom-0 left-0 h-px w-full origin-left scale-x-0 bg-gradient-to-r from-transparent via-[#d7b06a] to-transparent transition duration-500 group-hover:scale-x-100" />

              <div className="relative z-10">
                <div className="mb-9 flex h-13 w-13 items-center justify-center rounded-2xl border border-[#d7b06a]/20 bg-[#d7b06a]/10 text-[#d7b06a]">
                  <Icon size={23} />
                </div>
                <h3 className="text-2xl font-semibold tracking-[-0.04em] text-white">
                  {item.title}
                </h3>
                <p className="mt-4 text-sm leading-7 text-white/54">
                  {item.text}
                </p>
              </div>
            </motion.div>
          )
        })}
      </section>

      <section className="mx-auto grid w-full max-w-[1180px] grid-cols-1 gap-4 px-6 pb-20 md:grid-cols-3 md:px-8">
        {t.process.map(([number, title, text]: [string, string, string], index: number) => (
          <motion.div
            key={number}
            className="relative overflow-hidden rounded-[30px] border border-white/10 bg-[#080807]/70 p-7 backdrop-blur-xl"
            initial={{opacity: 0, y: 22}}
            whileInView={{opacity: 1, y: 0}}
            viewport={{once: true, margin: "-120px"}}
            transition={{duration: 0.55, delay: index * 0.08, ease: easePremium}}
          >
            <div className="mb-8 flex items-center justify-between">
              <span className="text-4xl font-black tracking-[-0.08em] text-[#d7b06a]">{number}</span>
              <MousePointer2 className="text-white/24" size={20} />
            </div>
            <h3 className="text-xl font-semibold tracking-[-0.04em]">{title}</h3>
            <p className="mt-4 text-sm leading-7 text-white/50">{text}</p>
          </motion.div>
        ))}
      </section>

      <section className="mx-auto w-full max-w-[1180px] px-6 pb-28 md:px-8">
        <motion.div
          className="relative overflow-hidden rounded-[38px] border border-[#d7b06a]/20 bg-[linear-gradient(135deg,rgba(215,176,106,0.16),rgba(255,255,255,0.035),rgba(0,0,0,0.25))] p-8 text-center shadow-[0_40px_140px_rgba(0,0,0,0.55)] backdrop-blur-2xl md:p-12"
          initial={{opacity: 0, y: 26}}
          whileInView={{opacity: 1, y: 0}}
          viewport={{once: true, margin: "-120px"}}
          transition={{duration: 0.65, ease: easePremium}}
        >
          <Crown className="mx-auto mb-6 text-[#d7b06a]" size={30} />
          <h2 className="mx-auto max-w-[760px] text-[clamp(2.3rem,4.6vw,4.8rem)] font-semibold leading-[0.95] tracking-[-0.06em]">
            {t.ctaTitle}
          </h2>
          <p className="mx-auto mt-6 max-w-[620px] text-base leading-8 text-white/58">
            {t.ctaText}
          </p>
          <Link
            href="/contact"
            className="mt-8 inline-flex h-14 items-center gap-3 border border-[#d7b06a] bg-[#d7b06a] px-8 text-sm font-black text-black transition duration-300 hover:bg-[#f1cf8b]"
          >
            {t.ctaButton}
            <ArrowUpRight size={17} />
          </Link>
        </motion.div>
      </section>

      <div className="pointer-events-none fixed bottom-5 left-5 z-40 flex h-9 w-9 items-center justify-center rounded-full border border-white/10 bg-black/35 text-xs font-bold text-white/60 backdrop-blur-xl">
        <Sparkles size={15} />
      </div>
    </main>
  )
}